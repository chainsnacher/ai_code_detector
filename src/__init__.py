"""Top-level package initializer for the AI Code Detection System.

This file makes the `src` directory a Python package so that pickled
artifacts referencing modules like `src.models.ensemble_model` can be
imported reliably at runtime (e.g., when loading models in the web app).
"""


