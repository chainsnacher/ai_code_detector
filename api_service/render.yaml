services:
  - type: web
    name: survey-integrity-api
    env: python
    plan: free
    buildCommand: pip install -r api_service/requirements.txt
    startCommand: uvicorn api_service.app:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
